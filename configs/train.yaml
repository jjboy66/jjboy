train:
  epochs: 50
  batch_size: 16
  image_size: 640
  optimizer: adamw
  learning_rate: 0.0005
  augmentations:
    mosaic: true
    mixup: true
    copy_paste: true
  losses:
    focal_loss_gamma: 2.0
    class_reweight: true
