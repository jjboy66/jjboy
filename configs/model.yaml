project_name: VisionGuard-Lite
model:
  baseline: yolov8n
  high_precision: rt-detr-l
  confidence_threshold: 0.35
  nms_iou_threshold: 0.5
  soft_nms_sigma: 0.5
  use_wbf: true
deploy:
  export:
    - onnx
    - tensorrt
